# Config mínima para desenvolvimento local (Fase 2).
# Sem TLS/auth de propósito: broker só é exposto em localhost:1883 no
# docker-compose, não é o setup de produção (ver docs/hardware-cloud-integration.md).
listener 1883
allow_anonymous true

persistence false

log_dest stdout
